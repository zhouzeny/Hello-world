# 社会痛点信息收集与统计分析平台技术设计方案

## 1. 技术选型

- 前端：React + Vite
- 后端：Spring Boot 3 + Java 17
- 数据库：MySQL 8
- 缓存：Redis
- 图表：ECharts
- 登录鉴权：JWT + 角色权限控制
- 部署：Nginx + Spring Boot + MySQL + Redis

## 2. 总体架构

系统采用前后端分离架构，分为三层：

- 展示层：React 前端，负责用户填报和后台管理界面
- 业务层：Spring Boot 接口服务，负责业务处理、分类统计、权限控制
- 数据层：MySQL 持久化存储，Redis 缓存热点数据与验证码

## 3. 前端设计

### 3.1 前台用户端

- 首页介绍
- 痛点信息填报页
- 提交成功页
- 隐私说明页

### 3.2 后台管理端

- 登录页
- 数据总览仪表盘
- 痛点列表页
- 分类管理页
- 行业统计页
- 报告生成页
- 管理员管理页

### 3.3 前端目录建议

```text
src/
  api/
  assets/
  components/
  hooks/
  layouts/
  pages/
    public/
    admin/
  router/
  store/
  styles/
  utils/
```

## 4. 后端设计

### 4.1 模块划分

- 公开接口模块：接收前台痛点提交、获取基础配置
- 管理认证模块：管理员登录、退出、权限校验
- 痛点管理模块：查询、分类、修改、审核
- 统计分析模块：按分类、行业、时间维度生成统计结果
- 报表导出模块：导出统计报表和行业总结
- 系统安全模块：加密、日志、审计、异常处理

### 4.2 后端分层建议

```text
controller
service
service/impl
repository
entity
dto
vo
config
security
util
```

## 5. 核心数据表

### 5.1 痛点提交表 `pain_point_report`

- id
- scene_type
- industry_type
- content
- contact_way
- contact_info_encrypted
- submit_time
- status

### 5.2 管理员表 `admin_user`

- id
- username
- password_hash
- role
- status
- last_login_time

### 5.3 分类字典表 `pain_point_category`

- id
- category_name
- category_type
- sort_order

### 5.4 操作日志表 `operation_log`

- id
- operator_id
- action
- target
- ip_address
- created_at

## 6. 接口设计

### 6.1 前台接口

- `GET /api/public/form-config`
- `POST /api/public/pain-points`

### 6.2 后台接口

- `POST /api/admin/auth/login`
- `POST /api/admin/auth/logout`
- `GET /api/admin/dashboard/stats`
- `GET /api/admin/pain-points`
- `PUT /api/admin/pain-points/{id}/category`
- `GET /api/admin/statistics/category`
- `GET /api/admin/statistics/industry`
- `GET /api/admin/reports/export`

## 7. 安全设计

- 前台到后端全程 HTTPS 传输
- 敏感字段应用层加密存储
- 管理端使用 JWT 登录态
- 接口按角色做权限校验
- 记录关键操作日志
- 对表单做基础防刷与频率限制

## 8. 部署建议

- `Nginx` 负责前端静态资源和反向代理
- `Spring Boot` 作为后端 API 服务
- `MySQL` 存储业务数据
- `Redis` 存储验证码、会话和统计缓存
- 后续可增加对象存储用于报表文件

## 9. 开发顺序建议

1. 先完成后端接口和数据库设计
2. 再完成 React 前台填报页
3. 然后开发后台管理端
4. 最后补统计图表、导出和权限控制

